{% for type, count_and_level in medals %}
    {% set level = count_and_level['Niveau'] %}
    <div class="medaille_profil">
        <img src="images/medailles/{{ type }}_{{ level }}_fond.png" /><br />
        <b>{{ _context['TITRE_MEDAILLE_'~(type|upper)] }}<br />{{ NIVEAU }} {{ level|replace({'avance': 'avancé'}) }}</b>
    </div>
{% endfor %}
<div class="clear">
    {% if issue_number > 0 %}
        {{ issue_number }} {{ NUMEROS }}<br />
        {{ publication_number }} {{ MAGAZINES }}<br />
        {{ country_number }} {{ PAYS }}<br />
    {% endif %}
</div>
